import ApiURL from "./ApiURL";

const UserApiURL = {
    usersApi: `${ApiURL.Api}/user/all`,
    getUserApi: `${ApiURL.Api}/user`,
    usersEditApi: `${ApiURL.Api}/user/update`,
}

export default UserApiURL;