import ApiURL from "./ApiURL";

const CategoryApiURL = {
    categoriesURL: `${ApiURL.Api}/category/all`,
    categoryCreate: `${ApiURL.Api}/category/create`,
    categoryURL: `${ApiURL.Api}/category`,
    categoryURLUpdate: `${ApiURL.Api}/category/update`,
    categoryURLDelte: `${ApiURL.Api}/category/delete`,
}

export default CategoryApiURL;